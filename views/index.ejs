<!-- 상단 소개글 -->
<div class="top">
  <h1 class="top-heading">2026 북중미 월드컵</h1>
  <p class="top-body">WE ARE 26! 최신 월드컵 경기 결과와 뜨거운 현장 기록을 확인하세요.</p>
</div>

<!-- 최근 경기 -->
<section class="articles">
  <h2 class="articles-heading">경기 목록</h2>
  <div class="match-grid">
    <% data.forEach(match => { %>
      <a href="/match/<%= match._id %>" class="match-card">
        <div class="match-stage"><%= match.stage %></div>
        <div class="match-teams">
          <span><%= match.homeTeam %></span>
          <span class="match-score">
            <%= match.homeScore !== null ? match.homeScore : '-' %> : <%= match.awayScore !== null ? match.awayScore : '-' %>
          </span>
          <span><%= match.awayTeam %></span>
        </div>
        <div class="match-date"><%= match.matchDate ? match.matchDate.toLocaleDateString() : '' %></div>
      </a>
    <% }) %>
  </div>
</section>